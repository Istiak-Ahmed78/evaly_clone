import 'package:flutter/material.dart';

class AccountTab extends StatelessWidget {
  static String navTitle = 'Account';
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Your name'),
    );
  }
}
